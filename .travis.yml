language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t aashiq2691/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aashiq2691/docker-react npm run test